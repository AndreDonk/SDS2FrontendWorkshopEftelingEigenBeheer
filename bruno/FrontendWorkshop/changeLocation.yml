info:
  name: changeLocation
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:8080/api/map/2/1
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const token = bru.getEnvVar("JWT_User");
        if (token) {
          req.setHeader("Authorization", `Bearer ${token}`);
        }
        console.log('token: ' + token)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
